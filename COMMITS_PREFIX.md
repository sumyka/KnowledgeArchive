# Conventional Commits 規約
Conventional Commits は、コミットメッセージの明確なルールを提供することで、プロジェクト履歴を読みやすくし、ツールの自動化を可能にする軽量な規約です。

## コミットメッセージのフォーマット
コミットメッセージは以下の構造に従います。

type:description

### 1. `type`（タイプ、必須）

変更の**種類**を示す単語です。主に以下のリストから選択します。

| タイプ | 説明 | セマンティックバージョニング |
| :--- | :--- | :--- |
| **`feat`** | **新機能**を導入する変更。 | **MINOR** バージョンの引き上げ（例: 1.2.0 - 1.3.0） |
| **`fix`** | **バグ修正**に対応する変更。 | **PATCH** バージョンの引き上げ（例: 1.2.1 - 1.2.2） |
| **`docs`** | ドキュメントのみの変更。 | バージョンの変更なし |
| **`style`** | コードの体裁、フォーマット（空白、セミコロンなど）に関する変更で、ロジックに影響しない。 | バージョンの変更なし |
| **`refactor`** | リファクタリング。動作を変更せず、コードの構造を改善する変更。 | バージョンの変更なし |
| **`test`** | テストの追加や修正。製品コードの変更は含まない。 | バージョンの変更なし |
| **`chore`** | その他の雑多な変更。ビルドプロセスやヘルパーツールの変更など。 | バージョンの変更なし |
| **`perf`** | パフォーマンスを改善するコードの変更。 | バージョンの変更なし |
| **`lint`** | パフォーマンスを改善しないコードの変更。 | バージョンの変更なし |
| **`build`** | ビルドシステムや外部依存関係（スコープ、依存ファイル）に関する変更。 | バージョンの変更なし |
| **`ci`** | CI（継続的インテグレーション）の構成ファイルやスクリプトに関する変更。 | バージョンの変更なし |

### 2. `description`（概要、必須）
変更の簡潔で分かりやすい説明。

* 動詞の現在形から始める（例: `ユーザー認証を追加`、`ログアウト時の問題を修正`）。
* 句読点（ピリオドなど）で終わらせない。

#### ⚠️ 重大な変更（BREAKING CHANGE）
APIの破壊的な変更（後方互換性のない変更）がある場合は、フッターまたはボディのいずれかのセクションの先頭に大文字で `BREAKING CHANGE:` を記述する必要があります。
* `BREAKING CHANGE:` を含めたコミットは、**MAJOR** バージョンの引き上げ（例: 1.2.3 - 2.0.0）に相当します。

*例:*
BREAKING CHANGE: 環境変数の命名規則が変更されました。```ENV_OLD_NAME``` は削除され、```ENV_NEW_NAME``` に置き換えられました。

#### 📌 関連するIssueの参照
関連するIssueを参照するために使用します（例: `Closes #123`, `Fixes #456`）。
